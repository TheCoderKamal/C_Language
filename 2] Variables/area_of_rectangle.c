// Q.2 Write a Program to find the area of a rectangle.

#include<stdio.h>
int main(){
    int length = 10;
    int breadth = 20;
    int area = length * breadth;

    printf("The area of the rectangle : %d units", area);
    return 0;
}
